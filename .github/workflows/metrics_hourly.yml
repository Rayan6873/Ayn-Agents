name: metrics_hourly
on:
  schedule:
    - cron: "0 * * * *"
jobs:
  dispatch:
    uses: ./.github/workflows/agent-runner.yml
    with:
      run_id: "scheduled"
      agent_name: "metrics_hourly"
      params_json: "{}"
